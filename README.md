# NeuralFlow AI

NeuralFlow AI is a React and Vite frontend for a data automation platform landing page.

## Run Locally

Prerequisite: Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`
3. Open:
   `http://localhost:3000`
